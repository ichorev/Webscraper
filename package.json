{
  "name": "iran-hotels-scraper",
  "version": "1.0.0",
  "description": "A web scraper for extracting hotel names from all cities in Iran on SnappTrip.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-browser": "npx puppeteer browsers install firefox",
    "postinstall": "npm run install-browser",
    "build": "npm install && npm run install-browser",
    "scrape": "node server.js"
  },
  "dependencies": {
    "puppeteer-firefox": "^0.5.1",
    "csv-writer": "^1.6.0"
  },
  "engines": {
    "node": "18.x"
  },
  "license": "MIT"
}
